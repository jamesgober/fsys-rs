//! Pipeline infrastructure — reserved for `0.4.0`.
//!
//! This module will house the solo-lane and group-commit dispatch logic.
//! Nothing is implemented here in `0.3.0`; the files exist as placeholders
//! so the module tree is stable across the 0.3.x series.
